# راهنمای ادمین (فارسی)

## تنظیم اولیه

1. به ربات `/myid` بفرستید — شناسه عددی شما نمایش داده می‌شود.
2. در فایل `.env` روی سرور قرار دهید:
   ```
   ADMIN_ID=123456789
   ```
3. ربات را **ری‌استارت** کنید.
4. در لاگ باید ببینید: `Admin IDs loaded: 123456789`

اگر `ADMIN_ID` خالی باشد، **هیچ پیام سفارش/پرداختی به ادمین نمی‌رسد.**

چند ادمین:
```
ADMIN_IDS=111111111,222222222
```

---

## دستورات

| دستور | کاربرد |
|--------|--------|
| `/admin` | پنل مدیریت با دکمه‌ها |
| `/user 123456789` | پروفایل کاربر + ارسال کانفیگ برای هر سفارش معلق |
| `/myid` | شناسه تلگرام شما |
| `/help` | راهنمای ادمین (فقط برای ادمین) |

---

## کدهای یکتا در دیتابیس

| نوع | فرمت | مثال |
|-----|------|------|
| شارژ کیف پول | `PAY-XXXXXXXX` | PAY-00000001 |
| سفارش VPN | `ORD-XXXXXXXX` | ORD-00000005 |
| اشتراک فعال | `SUB-XXXXXXXX` | SUB-00000012 |

هر رکورد هم `id` عددی داخلی SQLite دارد.

---

## شارژ کیف پول (کاربر)

1. کاربر: شارژ کیف پول → مبلغ → عکس رسید
2. ادمین: پیام با عکس + کد `PAY-...` + دکمه‌های ✅ تأیید / ❌ رد
3. **تأیید** → موجودی کاربر اضافه می‌شود

---

## سفارش VPN (کاربر)

1. کاربر: خرید پلن → تأیید (موجودی کافی باشد)
2. ادمین: پیام «سفارش جدید» با کد `ORD-...` + دکمه‌ها
3. **تأیید — ارسال کانفیگ** → در **پیام بعدی** لینک VLESS/vmess را بفرستید
4. ربات کانفیگ را برای کاربر می‌فرستد و مبلغ از کیف پول **کسر** می‌شود
5. **رد سفارش** → بدون کسر وجه

### پنل `/user` (پیشنهادی برای چند سفارش)

```
/user 123456789
```

- **ارسال کانفیگ (N)** — هر سفارش معلق یک دکمه (مثلاً ۵ سفارش = ۵ دکمه)
- کلیک روی `ORD-...` → پیام بعدی = لینک VPN
- **سفارش‌های موفق** — لیست + دکمه **مشاهده کانفیگ** داده‌شده
- **رد شده** — فقط لیست
- **تغییر موجودی** — همانند قبل

---

## پنل `/admin`

- **کاربران** — موجودی (`50000` یا `+10000` / `-5000`)، مسدود/آزاد
- **تأیید پرداخت‌ها** — لیست `PAY-...` معلق
- **سفارش‌ها** — لیست `ORD-...` معلق
- **پلن‌ها** — افزودن / حذف
- **کانفیگ‌ها** — ذخیره لیست لینک (اختیاری)
- **پیام همگانی** — یک متن برای همه
- **آمار**
